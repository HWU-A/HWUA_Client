import React from "react";
import styled from "styled-components";

const FooterPosition = styled.div`
  position: fixed;
  bottom: 0;
`;

const Footer = () => {
  return <FooterPosition>Footer</FooterPosition>;
};

export default Footer;
